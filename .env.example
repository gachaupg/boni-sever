MONGODB_URL=mongodb+srv://...
JWT_SECRET=your-secret-key-change-in-production
PORT=3001

# Optional: absolute path where Multer saves images (use a persistent disk mount in production).
# UPLOADS_DIR=/var/render/data/uploads
